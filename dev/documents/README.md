# Documents Legacy Entry

`dev/documents/` is now a legacy redirect surface.

Canonical homes are now:

- reports -> `dev/reports/`
- handbooks -> `dev/handbooks/`
- archive -> `dev/archive/`

Current report slices already split out:

- `dev/reports/analysis/`
- `dev/reports/branching/`
- `dev/reports/repo/`
- `dev/reports/runtime/`

Use:

- [INDEX.md](INDEX.md)

to find redirect notes and the new canonical report slices.
